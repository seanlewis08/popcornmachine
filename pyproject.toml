[project]
name = "popcorn-remake"
version = "0.1.0"
description = "NBA game stats data pipeline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "nba_api",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
